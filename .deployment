# Azure App Service Deployment Configuration

# Application Settings (set these in Azure App Service Configuration)
# WEBSITE_RUN_FROM_PACKAGE=1
# SCM_DO_BUILD_DURING_DEPLOYMENT=true
# ENABLE_ORYX_BUILD=true

# Python version
python-3.11

# Startup command for Azure App Service
# This will be used in the Azure portal startup command field:
# gunicorn --bind=0.0.0.0 --timeout 600 main_azure:app

# Alternative startup command for development:
# python main_azure.py